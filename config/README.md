This folder is part of the published repository structure.

Expected runtime file:

- `config.json`

Notes:

- the real runtime `config.json` is intentionally excluded from Git
- use `spo-backup-final/config.example.json` as the public-safe template
- the application normally mounts this folder into the containers through `spo-backup-final/docker-compose.yml`
